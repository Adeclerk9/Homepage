const createFunction = (Name, HP, ...Moves) => {
    let hp = HP;
    let name = Name;
    let moves = Moves;

    let x = {
        name: name,
        hp: hp,
     //  var mv= document.getElementsByClassName("moves")
     //  for(let move of moves){
     //   const newP = document.createElement('p');
     //   const text = document.createTextNode(move)
     //   newP.appendChild(text)
     //   parent.insertBefore(newP,mv)
     //
     //
     //
     //
     //
     //
     //
    }
}

